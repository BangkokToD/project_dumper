[pytest]
addopts = -q
testpaths = tests
python_files = test_*.py

filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

# На будущее: можно помечать GUI-тесты как "gui" и запускать их отдельно:
# markers =
#     gui: tests that require a running Qt application
